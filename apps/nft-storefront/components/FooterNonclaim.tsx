/** Locked synthetic nonclaim — must appear in footer on every page. */
export function FooterNonclaim() {
  return (
    <p className="px-6 pb-6 pt-3 text-center text-[11.5px] font-semibold leading-relaxed text-faint sm:px-8">
      Vitrine collectibles are synthetic — for the collecting experience, not real ownership, transfer, or financial value.
    </p>
  )
}
