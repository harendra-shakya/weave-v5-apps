# Procedure: 03-selection

Record the technology and architecture selection: Next.js 14 + TypeScript + Tailwind, `packages/commerce-core` as data layer, UI from `docs/atm-415/ui/<brand>` ported verbatim. If the brand forces a structural change the envelope does not support, stop with ENGINEERING_REQUIRED.

**Evidence required:** `apps/<app>/proof/selection-eval-result.json`.
