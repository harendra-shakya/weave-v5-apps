# Procedure: 07-deployment

**OWNER_GATE — always skipped in this envelope.**

Record `owner_gated_not_pursued` in lifecycle-state.json. Deployment requires provider account access, DNS authority, and billing — all outside the local envelope. No deployment action is taken.
